{
    "name": "performance",
    "author": "xcfio",
    "homepage": "https://github.com/xcfio/performance#readme",
    "license": "MIT",
    "version": "0.0.1",
    "scripts": {
        "build": "turbo run build",
        "clean": "node --eval \"const fs=require('node:fs');const path=require('node:path');function walk(d){for(const f of fs.readdirSync(d)){const p=path.join(d,f);if(fs.statSync(p).isDirectory()){if(f==='node_modules'){console.log('Removing',p);fs.rmSync(p,{recursive:true,force:true});console.log('Removed',p)}else{walk(p)}}}}console.log('Scanning...');walk('.');console.log('Clean done')\"",
        "clean:no-out": "node --eval \"const fs=require('node:fs');function walk(d){for(const f of fs.readdirSync(d)){const p=require('node:path').join(d,f);if(fs.statSync(p).isDirectory()){if(f==='node_modules'){fs.rmSync(p,{recursive:true,force:true})}else{walk(p)}}}}walk('.')\"",
        "dev": "turbo watch dev",
        "fmt": "prettier --config=.prettierrc --write .",
        "fmt:test": "prettier --config=.prettierrc --check src",
        "lint": "oxlint --config=oxlint.config.mts .",
        "lint:fix": "oxlint --config=oxlint.config.mts --fix .",
        "preinstall": "npx only-allow pnpm",
        "start": "turbo run start",
        "studio": "turbo run studio",
        "test": "turbo run test"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/xcfio/performance"
    },
    "bugs": {
        "url": "https://github.com/xcfio/performance/issues",
        "email": "xcfio@proton.me"
    },
    "devDependencies": {
        "@commitlint/config-conventional": "^21.2.2",
        "@commitlint/format": "^21.2.2",
        "@commitlint/types": "^21.2.0",
        "commitlint": "^21.2.2",
        "lefthook": "^2.1.12",
        "oxlint": "^1.80.0",
        "oxlint-plugin-eslint": "^1.80.0",
        "oxlint-tsgolint": "^7.0.2001",
        "prettier": "^3.9.6",
        "turbo": "^2.10.12",
        "typescript": "7.0.2"
    },
    "packageManager": "pnpm@11.21.0",
    "engines": {
        "node": ">=26.2"
    }
}
